name: test1
description: "A new Flutter project."
publish_to: 'none' # Убрать эту строку для публикации на pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Добавьте этот раздел для локальных изображений
  assets:
    - assets/HF.jpeg
    # Или можно добавить всю папку:
    # - assets/images/